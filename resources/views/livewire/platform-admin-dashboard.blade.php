<div class="min-h-screen bg-zinc-50 p-6 dark:bg-zinc-950"><div class="mx-auto max-w-7xl"><div class="flex items-end justify-between"><div><p class="text-sm font-semibold text-indigo-600">FINDER / ADMIN</p><h1 class="mt-1 text-3xl font-bold">Visão geral da plataforma</h1><p class="mt-2 text-sm text-zinc-500">Dados reais da plataforma, sem métricas fictícias.</p></div><a href="{{ route('dashboard') }}" class="rounded-xl border border-zinc-200 bg-white px-4 py-2 text-sm dark:border-zinc-800 dark:bg-zinc-900">Voltar ao dashboard</a></div><div class="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">@foreach([['Utilizadores',$usersCount],['Websites',$sitesCount],['Publicados',$publishedCount],['Rascunhos',$draftCount]] as $stat)<div class="rounded-2xl border border-zinc-200 bg-white p-5 shadow-sm dark:border-zinc-800 dark:bg-zinc-900"><p class="text-sm text-zinc-500">{{ $stat[0] }}</p><p class="mt-2 text-3xl font-semibold">{{ number_format($stat[1]) }}</p></div>@endforeach</div><div class="mt-8 grid gap-6 lg:grid-cols-2"><section class="rounded-2xl border border-zinc-200 bg-white p-6 dark:border-zinc-800 dark:bg-zinc-900"><h2 class="font-semibold">Websites recentes</h2><div class="mt-4 divide-y divide-zinc-100 dark:divide-zinc-800">@forelse($recentSites as $site)<div class="flex items-center justify-between gap-4 py-3"><div><p class="text-sm font-medium">{{ $site->name }}</p><p class="text-xs text-zinc-500">{{ $site->owner?->email ?? 'Sem proprietário' }}</p></div><div class="flex items-center gap-3"><a href="{{ route('admin.site.dashboard', $site) }}" class="text-xs font-semibold text-indigo-600">Gerir</a><form method="POST" action="{{ route('admin.websites.access', $site) }}">@csrf<button type="submit" class="text-xs font-semibold text-zinc-700 dark:text-zinc-200">Aceder como admin</button></form></div></div>@empty<p class="py-8 text-center text-sm text-zinc-500">Ainda não existem websites.</p>@endforelse</div></section><section class="rounded-2xl border border-zinc-200 bg-white p-6 dark:border-zinc-800 dark:bg-zinc-900"><h2 class="font-semibold">Utilizadores recentes</h2><div class="mt-4 divide-y divide-zinc-100 dark:divide-zinc-800">@forelse($newUsers as $user)<div class="flex items-center justify-between py-3"><div><p class="text-sm font-medium">{{ $user->name }}</p><p class="text-xs text-zinc-500">{{ $user->email }}</p></div><span class="text-xs text-zinc-400">{{ $user->created_at?->format('d/m/Y') }}</span></div>@empty<p class="py-8 text-center text-sm text-zinc-500">Ainda não existem utilizadores.</p>@endforelse</div></section></div></div></div>
