<div
    x-data="{ add: false, settings: false, inspectorTab: 'content', dragging: null, autosaveTimer: null, autosaveLabel: 'Guardado', queueAutosave() { this.autosaveLabel = 'Alterações por guardar'; clearTimeout(this.autosaveTimer); this.autosaveTimer = setTimeout(() => { this.autosaveLabel = 'A guardar…'; $wire.save().then(() => this.autosaveLabel = 'Guardado').catch(() => this.autosaveLabel = 'Erro ao guardar'); }, 900); }, dropSection(target) { if (this.dragging === null || this.dragging === target) return; const ids = [...this.$refs.canvas.querySelectorAll('[data-section-id]')].map(el => el.dataset.sectionId); const moved = ids.splice(this.dragging, 1)[0]; ids.splice(target, 0, moved); $wire.reorderSections(ids); this.dragging = null; } }"
    x-on:builder-dirty.window="queueAutosave()"
    x-on:builder-saved.window="autosaveLabel = 'Guardado'"
    class="flex h-screen min-h-0 flex-col overflow-hidden bg-zinc-100 text-zinc-900 dark:bg-zinc-950 dark:text-zinc-100"
>
    <header class="z-40 shrink-0 border-b border-zinc-200 bg-white/95 backdrop-blur dark:border-zinc-800 dark:bg-zinc-900/95">
        <div class="flex min-h-16 items-center gap-3 px-4 lg:px-6">
            <a href="{{ route('admin.site.dashboard', $site) }}" class="shrink-0 rounded-xl border border-zinc-200 px-3 py-2 text-sm font-bold hover:bg-zinc-50 dark:border-zinc-700 dark:hover:bg-zinc-800">← Sair</a>
            <div class="hidden min-w-0 lg:block"><p class="text-[10px] font-black uppercase tracking-widest text-indigo-600">Finder · Website Builder</p><p class="truncate text-sm font-black">{{ $site->name }}</p></div>
            <div class="ml-auto flex items-center gap-2">
                <select wire:model.live="pageId" wire:change="selectPage($event.target.value)" class="max-w-44 rounded-xl border-zinc-200 bg-white text-sm font-bold dark:border-zinc-700 dark:bg-zinc-800">
                    @foreach($site->pages->sortBy('sort_order') as $page)<option value="{{ $page->id }}">{{ $page->name }}{{ $page->is_homepage ? ' · Home' : '' }}</option>@endforeach
                </select>
                <button type="button" wire:click="createPage" class="hidden rounded-xl border px-3 py-2 text-xs font-bold lg:block">+ Página</button>
                <button type="button" wire:click="duplicatePage" class="hidden rounded-xl border px-3 py-2 text-xs font-bold xl:block">Duplicar</button>
                <button type="button" wire:click="undo" class="rounded-xl border px-3 py-2 text-xs font-bold" @disabled(!$this->canUndo)>↶</button>
                <button type="button" wire:click="redo" class="rounded-xl border px-3 py-2 text-xs font-bold" @disabled(!$this->canRedo)>↷</button>
                <span class="hidden text-xs font-semibold text-zinc-400 sm:block" x-text="autosaveLabel"></span>
                <a href="{{ route('site.public', ['site' => $site, 'preview' => 1]) }}" target="_blank" class="hidden rounded-xl border px-3 py-2 text-xs font-bold md:block">Pré-visualizar</a>
                <button type="button" wire:click="publish" class="rounded-xl bg-zinc-950 px-4 py-2 text-xs font-black text-white hover:bg-zinc-800 dark:bg-white dark:text-zinc-950">Publicar</button>
            </div>
        </div>
    </header>

    <div class="flex min-h-0 flex-1 overflow-hidden">
        <aside class="builder-scroll hidden h-full w-72 shrink-0 overflow-y-auto overscroll-contain border-r border-zinc-200 bg-white lg:block dark:border-zinc-800 dark:bg-zinc-900">
            <div class="border-b border-zinc-100 p-5 dark:border-zinc-800"><p class="text-[10px] font-black uppercase tracking-widest text-indigo-600">1 · Adicionar</p><h2 class="mt-1 text-lg font-black">Construir página</h2><p class="mt-1 text-xs leading-5 text-zinc-500">Adiciona blocos e personaliza-os sem código.</p></div>
            <div class="space-y-5 p-4">
                @foreach(['Conteúdo'=>['hero'=>'Hero / destaque','text'=>'Texto','image'=>'Imagem','button'=>'Botão','card'=>'Cartão'],'Estrutura'=>['feature_grid'=>'Benefícios','gallery'=>'Galeria','testimonials'=>'Testemunhos','faq'=>'Perguntas frequentes','pricing'=>'Preços'],'Negócio'=>['contact_form'=>'Formulário de contacto','product_grid'=>'Produtos','blog_posts'=>'Artigos','newsletter'=>'Newsletter','cta'=>'Call to action'],'Avançado'=>['video'=>'Vídeo','map'=>'Mapa','social_links'=>'Redes sociais']] as $group=>$types)
                    <div><p class="mb-2 px-1 text-[10px] font-black uppercase tracking-wider text-zinc-400">{{ $group }}</p><div class="grid gap-1.5">@foreach($types as $type=>$label)<button type="button" wire:click="addSection('{{ $type }}')" class="group flex items-center justify-between rounded-xl border border-zinc-200 bg-white px-3 py-3 text-left transition hover:border-indigo-300 hover:bg-indigo-50 dark:border-zinc-700 dark:bg-zinc-900 dark:hover:bg-zinc-800"><span><span class="block text-xs font-bold">{{ $label }}</span><span class="block text-[10px] text-zinc-400">Adicionar ao website</span></span><span class="text-zinc-300 group-hover:text-indigo-600">+</span></button>@endforeach</div></div>
                @endforeach
            </div>
        </aside>

        <main class="min-h-0 min-w-0 flex-1 overflow-y-auto overscroll-contain">
            <div class="border-b border-zinc-200 bg-white px-4 py-3 dark:border-zinc-800 dark:bg-zinc-900"><div class="mx-auto flex max-w-[1440px] flex-wrap items-center gap-3"><div class="min-w-0 flex-1"><div class="flex items-center gap-2"><input wire:model.live.debounce.500ms="pageName" aria-label="Nome da página" class="min-w-0 border-0 bg-transparent p-0 text-lg font-black outline-none focus:ring-0"><span class="rounded-full px-2 py-1 text-[9px] font-black uppercase {{ $pageStatus === 'published' ? 'bg-emerald-50 text-emerald-700' : 'bg-amber-50 text-amber-700' }}">{{ $pageStatus === 'published' ? 'Publicada' : 'Rascunho' }}</span></div><p class="text-xs text-zinc-400">/{{ trim($pageSlug, '/') }}</p></div><livewire:builder-templates :key="'builder-templates-'.$site->id" /><button type="button" wire:click="save" class="rounded-xl border px-4 py-2 text-xs font-black">Guardar agora</button><button type="button" wire:click="deletePage" class="rounded-xl border border-red-200 px-3 py-2 text-xs font-bold text-red-600">Eliminar página</button></div></div>
            <div class="mx-auto flex max-w-[1440px] justify-center p-4 lg:p-8"><div class="w-full max-w-[1200px] overflow-hidden rounded-2xl border border-zinc-200 bg-white shadow-sm dark:border-zinc-800 dark:bg-zinc-900" style="max-width: {{ $theme['content_width'] ?? '1200px' }}"><div x-ref="canvas" class="min-h-[650px]">
                @forelse($sections as $index=>$section)
                    @php($content=$section['content'] ?? [])
                    @php($items=$content['items'] ?? [])
                    <section data-section-id="{{ $section['id'] ?? 'new-'.$index }}" draggable="true" wire:key="builder-section-{{ $section['id'] ?? 'new-'.$index }}" @dragstart="dragging={{ $index }}" @dragover.prevent @drop.prevent="dropSection({{ $index }})" wire:click="selectSection({{ $index }})" class="group relative border-b border-zinc-100 transition dark:border-zinc-800 {{ $section['is_visible'] ? '' : 'opacity-40' }} {{ $selectedSection === $index ? 'ring-2 ring-inset ring-indigo-500' : 'hover:ring-1 hover:ring-indigo-300' }}">
                        <div class="absolute left-3 top-3 z-10 hidden items-center gap-1 rounded-lg border border-zinc-200 bg-white/95 p-1 shadow-sm group-hover:flex dark:border-zinc-700 dark:bg-zinc-900/95"><button type="button" wire:click.stop="selectSection({{ $index }})" class="rounded px-2 py-1 text-[10px] font-bold">Editar</button><button type="button" wire:click.stop="moveSection({{ $index }},'up')" class="rounded px-2 py-1 text-[10px]">↑</button><button type="button" wire:click.stop="moveSection({{ $index }},'down')" class="rounded px-2 py-1 text-[10px]">↓</button><button type="button" wire:click.stop="duplicateSection({{ $index }})" class="rounded px-2 py-1 text-[10px]">Duplicar</button><button type="button" wire:click.stop="setSectionVisibility({{ $index }},{{ $section['is_visible'] ? 'false' : 'true' }})" class="rounded px-2 py-1 text-[10px]">{{ $section['is_visible'] ? 'Ocultar' : 'Mostrar' }}</button><button type="button" wire:click.stop="removeSection({{ $index }})" class="rounded px-2 py-1 text-[10px] text-red-600">Eliminar</button></div>
                        <div class="px-6 py-14 lg:px-16">
                            @if($section['type'] === 'hero')<div class="mx-auto max-w-4xl text-center"><p class="mb-3 text-xs font-black uppercase tracking-[.25em]" style="color: {{ $theme['primary'] ?? '#635bff' }}">{{ $content['subtitle'] ?? 'Destaque' }}</p><h1 class="text-4xl font-black tracking-tight sm:text-6xl" style="color: {{ $theme['text'] ?? '#111827' }}">{{ $content['title'] ?? 'Título principal' }}</h1><p class="mx-auto mt-5 max-w-2xl text-base leading-7 text-zinc-500">{{ $content['description'] ?? $content['body'] ?? 'Apresenta aqui a tua empresa, produto ou serviço.' }}</p>@if(!empty($content['button_label']))<span class="mt-7 inline-flex rounded-xl px-5 py-3 text-sm font-black text-white" style="background: {{ $theme['primary'] ?? '#635bff' }}">{{ $content['button_label'] }}</span>@endif</div>
                            @elseif($section['type'] === 'text')<div class="mx-auto max-w-3xl"><h2 class="text-3xl font-black">{{ $content['title'] ?? 'Título' }}</h2><p class="mt-4 whitespace-pre-line leading-7 text-zinc-500">{{ $content['body'] ?? $content['description'] ?? '' }}</p></div>
                            @elseif($section['type'] === 'image')<div class="mx-auto max-w-4xl text-center">@if(!empty($content['url']))<img src="{{ $content['url'] }}" alt="{{ $content['alt'] ?? '' }}" class="mx-auto max-h-[480px] rounded-2xl object-cover shadow-sm">@else<div class="rounded-2xl border-2 border-dashed border-zinc-200 py-24 text-sm text-zinc-400">Adiciona o URL de uma imagem no painel da direita.</div>@endif@if(!empty($content['caption']))<p class="mt-3 text-xs text-zinc-400">{{ $content['caption'] }}</p>@endif</div>
                            @elseif($section['type'] === 'cta')<div class="rounded-3xl p-8 text-center sm:p-14" style="background: {{ $theme['background'] ?? '#f4f4f5' }}"><h2 class="text-3xl font-black">{{ $content['title'] ?? 'Pronto para começar?' }}</h2><p class="mx-auto mt-3 max-w-2xl text-zinc-500">{{ $content['description'] ?? '' }}</p><span class="mt-6 inline-flex rounded-xl px-5 py-3 text-sm font-black text-white" style="background: {{ $theme['primary'] ?? '#635bff' }}">{{ $content['button_label'] ?? 'Começar' }}</span></div>
                            @elseif(in_array($section['type'], ['feature_grid','card','testimonials','faq','pricing','gallery','product_grid','product_card','blog_posts','social_links'], true))<div><h2 class="text-center text-3xl font-black">{{ $content['title'] ?? \Illuminate\Support\Str::headline($section['type']) }}</h2>@if(!empty($content['description']))<p class="mx-auto mt-3 max-w-2xl text-center text-zinc-500">{{ $content['description'] }}</p>@endif<div class="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">@forelse($items as $item)<div class="rounded-2xl border border-zinc-200 p-5 dark:border-zinc-700"><p class="font-black">{{ $item['title'] ?? $item['name'] ?? $item['question'] ?? $item['label'] ?? 'Item' }}</p><p class="mt-2 text-sm leading-6 text-zinc-500">{{ $item['description'] ?? $item['quote'] ?? $item['answer'] ?? $item['excerpt'] ?? $item['price'] ?? '' }}</p></div>@empty<div class="col-span-full rounded-2xl border-2 border-dashed border-zinc-200 py-12 text-center text-sm text-zinc-400">Ainda não existem itens nesta secção.</div>@endforelse</div></div>
                            @else<div class="mx-auto max-w-3xl text-center"><h2 class="text-3xl font-black">{{ $content['title'] ?? \Illuminate\Support\Str::headline($section['type']) }}</h2><p class="mt-4 text-zinc-500">{{ $content['description'] ?? $content['body'] ?? $content['address'] ?? 'Personaliza esta secção no painel da direita.' }}</p></div>@endif
                        </div>
                    </section>
                @empty<div class="flex min-h-[650px] items-center justify-center p-8 text-center"><div><div class="mx-auto flex h-14 w-14 items-center justify-center rounded-2xl bg-indigo-50 text-2xl">✦</div><h2 class="mt-5 text-xl font-black">Começa a construir a tua página</h2><p class="mx-auto mt-2 max-w-md text-sm leading-6 text-zinc-500">Escolhe uma secção no painel esquerdo. O conteúdo aparece aqui e pode ser personalizado sem código.</p></div></div>@endforelse
            </div></div></div>
        </main>

        <aside class="builder-scroll hidden h-full w-[390px] shrink-0 overflow-y-auto overscroll-contain border-l border-zinc-200 bg-white lg:block dark:border-zinc-800 dark:bg-zinc-900">
            <div class="sticky top-0 z-20 border-b border-zinc-200 bg-white/95 backdrop-blur dark:border-zinc-800 dark:bg-zinc-900/95">
                <div class="p-5"><div class="flex items-start justify-between gap-3"><div><p class="text-[10px] font-black uppercase tracking-[.18em] text-indigo-600">3 · Personalizar</p><h2 class="mt-1 text-lg font-black leading-tight">Conteúdo da página</h2><p class="mt-2 text-xs leading-5 text-zinc-500">Aqui aparecem todos os campos existentes na secção selecionada.</p></div>@if($selectedSection !== null)<span class="mt-1 rounded-full bg-emerald-50 px-2 py-1 text-[9px] font-black uppercase text-emerald-700">Ativa</span>@endif</div></div>
                @if($selectedSection !== null && isset($sections[$selectedSection]))
                    <div class="grid grid-cols-2 border-t border-zinc-100 dark:border-zinc-800">
                        <button type="button" @click="inspectorTab='content'" :class="inspectorTab === 'content' ? 'border-indigo-500 text-indigo-600' : 'border-transparent text-zinc-500'" class="border-b-2 px-4 py-3 text-[10px] font-black uppercase tracking-wider">Conteúdo</button>
                        <button type="button" @click="inspectorTab='items'" :class="inspectorTab === 'items' ? 'border-indigo-500 text-indigo-600' : 'border-transparent text-zinc-500'" class="border-b-2 px-4 py-3 text-[10px] font-black uppercase tracking-wider">Itens</button>
                    </div>
                @endif
            </div>
            @if($selectedSection !== null && isset($sections[$selectedSection]))
                @php($selectedContent = $sections[$selectedSection]['content'] ?? [])
                @php($selectedItems = $selectedContent['items'] ?? [])
                <div class="space-y-5 p-5" x-show="inspectorTab === 'content'" x-cloak>
                    <div><label class="text-xs font-bold">Título</label><input wire:model.live.debounce.500ms="sections.{{ $selectedSection }}.content.title" class="mt-2 w-full rounded-xl border-zinc-200 bg-zinc-50 px-3 py-3 text-sm dark:border-zinc-700 dark:bg-zinc-800"></div>
                    <div><label class="text-xs font-bold">Subtítulo / etiqueta</label><input wire:model.live.debounce.500ms="sections.{{ $selectedSection }}.content.subtitle" class="mt-2 w-full rounded-xl border-zinc-200 bg-zinc-50 px-3 py-3 text-sm dark:border-zinc-700 dark:bg-zinc-800"></div>
                    <div><label class="text-xs font-bold">Descrição</label><textarea wire:model.live.debounce.500ms="sections.{{ $selectedSection }}.content.description" rows="5" class="mt-2 w-full rounded-xl border-zinc-200 bg-zinc-50 px-3 py-3 text-sm dark:border-zinc-700 dark:bg-zinc-800"></textarea></div>
                    <div class="rounded-2xl border border-zinc-200 p-4 dark:border-zinc-700"><p class="text-[10px] font-black uppercase tracking-wider text-zinc-400">Botão / ligação</p><div class="mt-3 space-y-3"><div><label class="text-xs font-bold">Texto</label><input wire:model.live.debounce.500ms="sections.{{ $selectedSection }}.content.button_label" class="mt-2 w-full rounded-xl border-zinc-200 px-3 py-2 text-sm dark:border-zinc-700 dark:bg-zinc-800"></div><div><label class="text-xs font-bold">URL</label><input wire:model.live.debounce.500ms="sections.{{ $selectedSection }}.content.button_url" placeholder="https://..." class="mt-2 w-full rounded-xl border-zinc-200 px-3 py-2 text-sm dark:border-zinc-700 dark:bg-zinc-800"></div></div></div>
                    <details class="rounded-2xl border border-zinc-200 p-4 dark:border-zinc-700"><summary class="cursor-pointer text-xs font-black">Imagem e acessibilidade</summary><div class="mt-4 space-y-3"><div><label class="text-xs font-bold">URL da imagem</label><input wire:model.live.debounce.500ms="sections.{{ $selectedSection }}.content.url" class="mt-2 w-full rounded-xl border-zinc-200 px-3 py-2 text-sm dark:border-zinc-700 dark:bg-zinc-800"></div><div><label class="text-xs font-bold">Legenda</label><input wire:model.live.debounce.500ms="sections.{{ $selectedSection }}.content.caption" class="mt-2 w-full rounded-xl border-zinc-200 px-3 py-2 text-sm dark:border-zinc-700 dark:bg-zinc-800"></div><div><label class="text-xs font-bold">Texto alternativo</label><input wire:model.live.debounce.500ms="sections.{{ $selectedSection }}.content.alt" class="mt-2 w-full rounded-xl border-zinc-200 px-3 py-2 text-sm dark:border-zinc-700 dark:bg-zinc-800"></div><div><label class="text-xs font-bold">Morada / localização</label><input wire:model.live.debounce.500ms="sections.{{ $selectedSection }}.content.address" class="mt-2 w-full rounded-xl border-zinc-200 px-3 py-2 text-sm dark:border-zinc-700 dark:bg-zinc-800"></div></div></details>
                </div>
                <div class="space-y-4 p-5" x-show="inspectorTab === 'items'" x-cloak>
                    <div class="flex items-center justify-between"><div><p class="text-xs font-black">Itens da secção</p><p class="mt-1 text-[11px] text-zinc-400">Edita cartões, benefícios, perguntas e outros itens.</p></div><button type="button" wire:click="addItem({{ $selectedSection }})" class="rounded-xl bg-zinc-950 px-3 py-2 text-[10px] font-black text-white dark:bg-white dark:text-zinc-950">+ Item</button></div>
                    @forelse($selectedItems as $itemIndex=>$item)
                        <div class="rounded-2xl border border-zinc-200 p-4 dark:border-zinc-700"><div class="flex items-center justify-between gap-2"><span class="text-[10px] font-black uppercase tracking-wider text-zinc-400">Item {{ $itemIndex + 1 }}</span><div class="flex gap-1"><button type="button" wire:click="moveItem({{ $selectedSection }},{{ $itemIndex }},'up')" class="rounded-lg border px-2 py-1 text-[10px]">↑</button><button type="button" wire:click="moveItem({{ $selectedSection }},{{ $itemIndex }},'down')" class="rounded-lg border px-2 py-1 text-[10px]">↓</button><button type="button" wire:click="removeItem({{ $selectedSection }},{{ $itemIndex }})" class="rounded-lg border border-red-200 px-2 py-1 text-[10px] text-red-600">Eliminar</button></div></div><input wire:model.live.debounce.500ms="sections.{{ $selectedSection }}.content.items.{{ $itemIndex }}.title" class="mt-3 w-full rounded-xl border-zinc-200 px-3 py-2 text-sm dark:border-zinc-700 dark:bg-zinc-800" placeholder="Título do item"><textarea wire:model.live.debounce.500ms="sections.{{ $selectedSection }}.content.items.{{ $itemIndex }}.description" rows="3" class="mt-2 w-full rounded-xl border-zinc-200 px-3 py-2 text-sm dark:border-zinc-700 dark:bg-zinc-800" placeholder="Descrição"></textarea></div>
                    @empty
                        <div class="rounded-2xl border-2 border-dashed border-zinc-200 p-8 text-center text-xs text-zinc-400 dark:border-zinc-700">Ainda não existem itens.</div>
                    @endforelse
                </div>
            @else
                <div class="flex min-h-[420px] items-center justify-center p-8 text-center"><div><div class="mx-auto flex h-12 w-12 items-center justify-center rounded-2xl bg-zinc-100 text-xl dark:bg-zinc-800">✦</div><h3 class="mt-4 font-black">Seleciona uma secção</h3><p class="mt-2 text-xs leading-5 text-zinc-500">Clica numa secção no centro para abrir todas as opções de personalização.</p></div></div>
            @endif
        </aside>
    </div>

    <div class="fixed inset-x-0 bottom-4 z-50 flex justify-center px-4 lg:hidden">
        <div class="flex w-full max-w-md gap-2 rounded-2xl border border-zinc-200 bg-white/95 p-2 shadow-2xl backdrop-blur dark:border-zinc-700 dark:bg-zinc-900/95">
            <button type="button" @click="add=true" class="flex-1 rounded-xl bg-zinc-950 px-3 py-3 text-xs font-black text-white dark:bg-white dark:text-zinc-950">1 · Adicionar</button>
            <button type="button" @click="settings=true" class="flex-1 rounded-xl border px-3 py-3 text-xs font-black">3 · Personalizar</button>
        </div>
    </div>

    <div x-show="add" x-cloak class="fixed inset-0 z-[70] bg-black/40 p-4 lg:hidden" @click.self="add=false"><div class="mx-auto mt-16 max-w-lg rounded-3xl bg-white p-5 shadow-2xl dark:bg-zinc-900"><div class="flex items-center justify-between"><h3 class="font-black">Adicionar secção</h3><button type="button" @click="add=false" class="rounded-xl border px-3 py-2">✕</button></div><div class="mt-5 grid grid-cols-2 gap-2">@foreach(['hero'=>'Hero','text'=>'Texto','image'=>'Imagem','feature_grid'=>'Benefícios','gallery'=>'Galeria','testimonials'=>'Testemunhos','faq'=>'FAQ','pricing'=>'Preços','contact_form'=>'Contacto','product_grid'=>'Produtos','blog_posts'=>'Artigos','newsletter'=>'Newsletter','cta'=>'CTA'] as $type=>$label)<button type="button" wire:click="addSection('{{ $type }}'); add=false" class="rounded-xl border px-3 py-3 text-left text-xs font-bold">{{ $label }}</button>@endforeach</div></div></div>

    <div x-show="settings" x-cloak class="fixed inset-0 z-[70] bg-black/40 p-4 lg:hidden" @click.self="settings=false"><div class="mx-auto mt-16 max-w-lg rounded-3xl bg-white p-5 shadow-2xl dark:bg-zinc-900"><div class="flex items-center justify-between"><h3 class="font-black">3 · Personalizar</h3><button type="button" @click="settings=false" class="rounded-xl border px-3 py-2">✕</button></div><p class="mt-3 text-sm text-zinc-500">No computador, usa o painel da direita para editar a secção selecionada.</p></div></div>
</div>