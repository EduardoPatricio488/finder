@php
    $elementGroups = [
        'Básicos' => ['hero', 'text', 'image', 'button'],
        'Conteúdo' => ['feature_grid', 'card', 'testimonials', 'faq', 'gallery'],
        'Negócio' => ['product_grid', 'product_card', 'pricing', 'blog_posts', 'contact_form', 'newsletter'],
        'Media' => ['video', 'map', 'social_links'],
        'Conversão' => ['cta'],
    ];
    $icons = ['hero'=>'sparkles','text'=>'document-text','image'=>'photo','button'=>'cursor-arrow-rays','feature_grid'=>'squares-2x2','card'=>'rectangle-stack','testimonials'=>'chat-bubble-left-right','faq'=>'question-mark-circle','gallery'=>'photo','product_grid'=>'shopping-bag','product_card'=>'tag','pricing'=>'currency-euro','blog_posts'=>'newspaper','contact_form'=>'envelope','newsletter'=>'envelope-open','video'=>'play-circle','map'=>'map-pin','social_links'=>'share','cta'=>'bolt'];
    $descriptions = ['hero'=>'Título principal, mensagem e chamada à ação.','text'=>'Título e texto explicativo.','image'=>'Imagem com legenda e texto alternativo.','button'=>'Botão com ligação.','feature_grid'=>'Benefícios ou características em cartões.','card'=>'Cartão de conteúdo simples.','testimonials'=>'Opiniões de clientes.','faq'=>'Perguntas e respostas.','gallery'=>'Galeria de imagens.','product_grid'=>'Lista automaticamente os teus produtos.','product_card'=>'Produto individual.','pricing'=>'Planos e preços.','blog_posts'=>'Artigos e notícias.','contact_form'=>'Formulário de contacto.','newsletter'=>'Inscrições na newsletter.','video'=>'Vídeo.','map'=>'Localização.','social_links'=>'Redes sociais.','cta'=>'Chamada à ação.'];
@endphp

<div class="flex h-screen min-h-0 flex-col bg-zinc-100 text-zinc-950 dark:bg-zinc-950 dark:text-white" x-data="{ add:false, settings:false, help:false, mobile:false, tablet:false, selected:0, preview:false }" x-on:keydown.escape.window="add=false;settings=false;help=false;preview=false">
    <style>[x-cloak]{display:none!important}.builder-scroll::-webkit-scrollbar{width:7px;height:7px}.builder-scroll::-webkit-scrollbar-thumb{background:rgba(113,113,122,.3);border-radius:999px}.inline-edit{outline:none}.inline-edit:focus{box-shadow:0 0 0 2px rgba(99,102,241,.25);border-radius:.4rem}</style>

    <header class="z-40 flex h-16 shrink-0 items-center gap-3 border-b border-zinc-200 bg-white px-3 shadow-sm dark:border-zinc-800 dark:bg-zinc-900 sm:px-4">
        <a href="{{ route('dashboard') }}" wire:navigate class="flex size-9 items-center justify-center rounded-xl border border-zinc-200 text-zinc-500 hover:bg-zinc-50 dark:border-zinc-700 dark:hover:bg-zinc-800" title="Voltar"><flux:icon name="arrow-left" class="size-4" /></a>
        <div class="min-w-0 flex-1"><p class="truncate text-sm font-black">{{ $site->name }}</p><p class="truncate text-[11px] text-zinc-400">A editar: <strong>{{ $pageName }}</strong></p></div>
        <div class="hidden rounded-full border border-zinc-200 bg-zinc-50 px-3 py-1.5 text-xs font-semibold md:block dark:border-zinc-700 dark:bg-zinc-800">@if($dirty)<span class="text-amber-600">● Alterações por guardar</span>@else<span class="text-emerald-600">● Tudo guardado</span>@endif</div>
        <button type="button" @click="help=true" class="flex size-9 items-center justify-center rounded-xl text-zinc-500 hover:bg-zinc-100" title="Como funciona"><flux:icon name="question-mark-circle" class="size-5" /></button>
        <div class="hidden items-center rounded-xl border border-zinc-200 sm:flex"><button type="button" wire:click="undo" @disabled(!$this->canUndo) class="size-9 text-zinc-500 disabled:opacity-30" title="Desfazer">↶</button><button type="button" wire:click="redo" @disabled(!$this->canRedo) class="size-9 text-zinc-500 disabled:opacity-30" title="Refazer">↷</button></div>
        <button type="button" @click="preview=true" class="hidden rounded-xl border border-zinc-200 px-3 py-2 text-xs font-bold sm:block">Pré-visualizar</button>
        <button type="button" wire:click="save" class="hidden rounded-xl bg-zinc-950 px-3 py-2 text-xs font-bold text-white sm:block dark:bg-white dark:text-zinc-950">Guardar</button>
        @if($site->is_published)<button type="button" wire:click="unpublish" class="hidden rounded-xl px-3 py-2 text-xs font-bold md:block">Despublicar</button>@else<button type="button" wire:click="publish" class="rounded-xl bg-indigo-600 px-3 py-2 text-xs font-bold text-white hover:bg-indigo-500">Publicar</button>@endif
    </header>

    <div class="flex min-h-0 flex-1">
        <aside class="builder-scroll hidden w-72 shrink-0 overflow-y-auto border-r border-zinc-200 bg-white lg:block dark:border-zinc-800 dark:bg-zinc-900">
            <div class="border-b border-zinc-100 p-4 dark:border-zinc-800"><p class="text-[10px] font-black uppercase tracking-widest text-indigo-600">1 · Adicionar</p><h2 class="mt-1 font-black">Elementos</h2><p class="mt-1 text-xs leading-5 text-zinc-500">Adiciona conteúdo à página e edita-o directamente no centro.</p></div>
            <div class="space-y-5 p-4">
                @foreach($elementGroups as $group=>$types)
                    <div><p class="mb-2 text-[10px] font-black uppercase tracking-widest text-zinc-400">{{ $group }}</p><div class="space-y-2">@foreach($types as $type)<button type="button" wire:click="addSection('{{ $type }}')" class="group flex w-full gap-3 rounded-xl border border-zinc-200 p-3 text-left hover:border-indigo-300 hover:bg-indigo-50/50 dark:border-zinc-700"><span class="flex size-9 shrink-0 items-center justify-center rounded-lg bg-zinc-100 text-zinc-500 group-hover:bg-indigo-600 group-hover:text-white dark:bg-zinc-800"><flux:icon :name="$icons[$type]" class="size-4" /></span><span><strong class="block text-xs">{{ \Illuminate\Support\Str::headline($type) }}</strong><small class="mt-0.5 block text-[10px] leading-4 text-zinc-400">{{ $descriptions[$type] }}</small></span></button>@endforeach</div></div>
                @endforeach
            </div>
        </aside>

        <main class="builder-scroll min-w-0 flex-1 overflow-y-auto bg-zinc-100 p-3 sm:p-6">
            <div class="mx-auto max-w-6xl">
                <div class="mb-4 flex flex-wrap items-center gap-3">
                    <div class="min-w-0 flex-1"><div class="flex items-center gap-2"><input wire:model.live.debounce.500ms="pageName" class="max-w-[240px] rounded-lg border-0 bg-transparent p-0 text-lg font-black outline-none focus:ring-2 focus:ring-indigo-500/20"><span class="rounded-md bg-zinc-200 px-2 py-1 text-[10px] text-zinc-500">/{{ $pageSlug }}</span></div><p class="mt-1 text-xs text-zinc-400">Clica directamente no texto para editar. Selecciona uma secção para opções avançadas.</p></div>
                    <div class="flex items-center gap-1 rounded-xl border border-zinc-200 bg-white p-1"><button type="button" @click="mobile=false;tablet=false" :class="!mobile&&!tablet?'bg-zinc-950 text-white':''" class="rounded-lg px-2.5 py-1.5 text-[10px] font-bold">Desktop</button><button type="button" @click="mobile=false;tablet=true" :class="tablet?'bg-zinc-950 text-white':''" class="rounded-lg px-2.5 py-1.5 text-[10px] font-bold">Tablet</button><button type="button" @click="mobile=true;tablet=false" :class="mobile?'bg-zinc-950 text-white':''" class="rounded-lg px-2.5 py-1.5 text-[10px] font-bold">Telemóvel</button></div>
                    <button type="button" @click="settings=true" class="rounded-xl border border-zinc-200 bg-white px-3 py-2 text-xs font-bold lg:hidden">Personalizar</button>
                </div>

                <div class="mx-auto overflow-hidden rounded-2xl border border-zinc-200 bg-white shadow-2xl shadow-zinc-900/10 transition-all" :class="mobile?'max-w-[390px]':(tablet?'max-w-[768px]':'max-w-none')">
                    <div class="flex items-center gap-2 border-b border-zinc-100 bg-zinc-50 px-4 py-2"><span class="size-2 rounded-full bg-zinc-300"></span><span class="size-2 rounded-full bg-zinc-300"></span><span class="size-2 rounded-full bg-zinc-300"></span><span class="ml-2 flex-1 text-center text-[10px] text-zinc-400">{{ $site->name }} / {{ $pageName }}</span></div>

                    @forelse($sections as $index=>$section)
                        @php($content=$section['content'] ?? [])
                        <section wire:key="section-{{ $section['id'] ?? 'new-'.$index }}" @click="selected={{ $index }};settings=true" :class="selected==={{ $index }}?'ring-2 ring-inset ring-indigo-500':''" class="group relative border-b border-zinc-100 p-7 transition hover:ring-2 hover:ring-inset hover:ring-indigo-300/60 sm:p-12 {{ !($section['is_visible']??true)?'opacity-50':'' }}" style="background:{{ $section['settings']['background']??'transparent' }};color:{{ $section['settings']['text_color']??'inherit' }}">
                            <div class="absolute left-3 top-3 z-10 rounded-lg bg-indigo-600 px-2 py-1 text-[9px] font-black text-white opacity-0 transition group-hover:opacity-100" :class="selected==={{ $index }}?'!opacity-100':''">{{ $index+1 }} · {{ \Illuminate\Support\Str::headline($section['type']) }}</div>
                            <div class="absolute right-3 top-3 z-20 flex gap-1 opacity-0 transition group-hover:opacity-100" :class="selected==={{ $index }}?'!opacity-100':''"><button type="button" wire:click.stop="moveSection({{ $index }},'up')" class="rounded-lg border bg-white px-2 py-1 text-[10px]" title="Subir">↑</button><button type="button" wire:click.stop="moveSection({{ $index }},'down')" class="rounded-lg border bg-white px-2 py-1 text-[10px]" title="Descer">↓</button><button type="button" wire:click.stop="duplicateSection({{ $index }})" class="rounded-lg border bg-white px-2 py-1 text-[10px]" title="Duplicar">⧉</button><button type="button" wire:click.stop="removeSection({{ $index }})" class="rounded-lg border border-red-200 bg-white px-2 py-1 text-[10px] text-red-600" title="Eliminar">×</button></div>

                            @if($section['type']==='hero')
                                <div class="mx-auto max-w-3xl text-center"><div contenteditable="true" class="inline-edit text-3xl font-black tracking-tight sm:text-5xl" @click.stop x-on:blur="$wire.updateInline({{ $index }},'title',$event.target.innerText)">{{ $content['title'] ?? 'Título principal' }}</div><div contenteditable="true" class="inline-edit mx-auto mt-4 max-w-2xl text-sm leading-6 text-zinc-500" @click.stop x-on:blur="$wire.updateInline({{ $index }},'subtitle',$event.target.innerText)">{{ $content['subtitle'] ?? 'Escreve uma mensagem para os visitantes.' }}</div><div class="mt-6"><span contenteditable="true" class="inline-edit inline-block rounded-xl bg-zinc-950 px-5 py-3 text-sm font-bold text-white dark:bg-white dark:text-zinc-950" @click.stop x-on:blur="$wire.updateInline({{ $index }},'button_label',$event.target.innerText)">{{ $content['button_label'] ?? 'Saber mais' }}</span></div></div>
                            @elseif($section['type']==='text')
                                <div class="mx-auto max-w-3xl"><div contenteditable="true" class="inline-edit text-2xl font-black" @click.stop x-on:blur="$wire.updateInline({{ $index }},'title',$event.target.innerText)">{{ $content['title'] ?? 'Título' }}</div><div contenteditable="true" class="inline-edit mt-4 whitespace-pre-line text-sm leading-7 text-zinc-600" @click.stop x-on:blur="$wire.updateInline({{ $index }},'body',$event.target.innerText)">{{ $content['body'] ?? 'Texto da secção.' }}</div></div>
                            @elseif($section['type']==='image')
                                <div class="mx-auto max-w-3xl text-center"><div class="flex min-h-48 items-center justify-center rounded-2xl border-2 border-dashed border-zinc-200 bg-zinc-50 text-sm text-zinc-400">{{ $content['url'] ? 'Imagem definida' : 'Clica em Personalizar para adicionar uma imagem' }}</div><div contenteditable="true" class="inline-edit mt-3 text-xs text-zinc-400" @click.stop x-on:blur="$wire.updateInline({{ $index }},'caption',$event.target.innerText)">{{ $content['caption'] ?? 'Legenda da imagem' }}</div></div>
                            @elseif($section['type']==='button')
                                <div class="text-center"><span contenteditable="true" class="inline-edit inline-block rounded-xl bg-indigo-600 px-6 py-3 text-sm font-bold text-white" @click.stop x-on:blur="$wire.updateInline({{ $index }},'label',$event.target.innerText)">{{ $content['label'] ?? 'Botão' }}</span></div>
                            @elseif(in_array($section['type'],['feature_grid','testimonials','faq','gallery','pricing','blog_posts','social_links'],true))
                                <div class="mx-auto max-w-5xl"><div contenteditable="true" class="inline-edit text-2xl font-black" @click.stop x-on:blur="$wire.updateInline({{ $index }},'title',$event.target.innerText)">{{ $content['title'] ?? \Illuminate\Support\Str::headline($section['type']) }}</div><div class="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
                                    @foreach(($content['items'] ?? []) as $itemIndex=>$item)
                                        <div class="rounded-2xl border border-zinc-200 bg-white p-5 shadow-sm dark:border-zinc-700 dark:bg-zinc-900" @click.stop><div contenteditable="true" class="inline-edit font-bold" @click.stop x-on:blur="$wire.updateInline({{ $index }},'{{ in_array($section['type'],['faq'])?'question':(in_array($section['type'],['testimonials'])?'name':(in_array($section['type'],['social_links'])?'label':'title')) }}',$event.target.innerText,{{ $itemIndex }})">{{ $item['title'] ?? $item['name'] ?? $item['question'] ?? $item['label'] ?? $item['name'] ?? 'Item' }}</div><div contenteditable="true" class="inline-edit mt-2 text-xs leading-5 text-zinc-500" @click.stop x-on:blur="$wire.updateInline({{ $index }},'{{ in_array($section['type'],['faq'])?'answer':(in_array($section['type'],['testimonials'])?'quote':(in_array($section['type'],['blog_posts'])?'excerpt':'description')) }}',$event.target.innerText,{{ $itemIndex }})">{{ $item['description'] ?? $item['quote'] ?? $item['answer'] ?? $item['excerpt'] ?? 'Descrição do conteúdo.' }}</div></div>
                                    @endforeach
                                </div><div class="mt-5 text-xs text-zinc-400">Clica num texto para editar. Usa o painel para adicionar ou remover itens.</div></div>
                            @elseif($section['type']==='contact_form')
                                <div class="mx-auto max-w-xl text-center"><div contenteditable="true" class="inline-edit text-2xl font-black" @click.stop x-on:blur="$wire.updateInline({{ $index }},'title',$event.target.innerText)">{{ $content['title'] ?? 'Contacta-nos' }}</div><div contenteditable="true" class="inline-edit mt-2 text-sm text-zinc-500" @click.stop x-on:blur="$wire.updateInline({{ $index }},'description',$event.target.innerText)">{{ $content['description'] ?? 'Envia-nos uma mensagem.' }}</div><div class="mt-5 grid gap-3"><div class="h-10 rounded-xl bg-zinc-100"></div><div class="h-24 rounded-xl bg-zinc-100"></div><div class="h-10 rounded-xl bg-zinc-950"></div></div></div>
                            @elseif($section['type']==='cta')
                                <div class="mx-auto max-w-3xl rounded-3xl bg-zinc-950 p-8 text-center text-white sm:p-12"><div contenteditable="true" class="inline-edit text-2xl font-black" @click.stop x-on:blur="$wire.updateInline({{ $index }},'title',$event.target.innerText)">{{ $content['title'] ?? 'Pronto para começar?' }}</div><div contenteditable="true" class="inline-edit mt-3 text-sm text-zinc-300" @click.stop x-on:blur="$wire.updateInline({{ $index }},'description',$event.target.innerText)">{{ $content['description'] ?? 'Descrição' }}</div><span contenteditable="true" class="inline-edit mt-5 inline-block rounded-xl bg-white px-5 py-3 text-sm font-bold text-zinc-950" @click.stop x-on:blur="$wire.updateInline({{ $index }},'button_label',$event.target.innerText)">{{ $content['button_label'] ?? 'Começar' }}</span></div>
                            @else
                                <div class="mx-auto max-w-4xl text-center"><div contenteditable="true" class="inline-edit text-2xl font-black" @click.stop x-on:blur="$wire.updateInline({{ $index }},'title',$event.target.innerText)">{{ $content['title'] ?? \Illuminate\Support\Str::headline($section['type']) }}</div><div contenteditable="true" class="inline-edit mx-auto mt-3 max-w-2xl text-sm text-zinc-500" @click.stop x-on:blur="$wire.updateInline({{ $index }},'description',$event.target.innerText)">{{ $content['description'] ?? 'Personaliza esta secção no painel.' }}</div></div>
                            @endif
                        </section>
                    @empty
                        <div class="p-16 text-center"><div class="mx-auto flex size-16 items-center justify-center rounded-2xl bg-indigo-50 text-indigo-600"><flux:icon name="sparkles" class="size-7" /></div><h2 class="mt-5 text-xl font-black">Vamos começar</h2><p class="mx-auto mt-2 max-w-md text-sm leading-6 text-zinc-500">Adiciona uma secção na coluna da esquerda. O website deixa de ser uma página vazia e passa a ser construído por blocos fáceis de editar.</p><button type="button" @click="add=true" class="mt-6 rounded-xl bg-zinc-950 px-5 py-3 text-sm font-bold text-white lg:hidden">Adicionar primeiro elemento</button></div>
                    @endforelse
                </div>
            </div>
        </main>

        <aside class="builder-scroll hidden w-80 shrink-0 overflow-y-auto border-l border-zinc-200 bg-white lg:block dark:border-zinc-800 dark:bg-zinc-900">
            <div class="border-b border-zinc-100 p-4 dark:border-zinc-800"><p class="text-[10px] font-black uppercase tracking-widest text-indigo-600">3 · Personalizar</p><h2 class="mt-1 font-black">{{ $sections[$this->selectedSection ?? 0]['label'] ?? 'Elemento seleccionado' }}</h2><p class="mt-1 text-xs leading-5 text-zinc-500">Altera o conteúdo aqui ou edita directamente no website.</p></div>
            <div class="space-y-6 p-4">
                @if(isset($sections[0]))
                    @php($sIndex=$this->selectedSection ?? 0)
                    @if(isset($sections[$sIndex]))
                        @php($s=$sections[$sIndex];$c=$s['content']??[])
                        <div><label class="text-xs font-bold">Nome interno</label><input wire:model.live="sections.{{ $sIndex }}.label" class="mt-2 w-full rounded-xl border-zinc-200 text-sm dark:border-zinc-700 dark:bg-zinc-800"></div>
                        @foreach(['title'=>'Título','subtitle'=>'Subtítulo','body'=>'Texto','description'=>'Descrição','button_label'=>'Texto do botão','button_url'=>'Ligação / URL','caption'=>'Legenda','alt'=>'Texto alternativo','address'=>'Morada'] as $field=>$label)
                            @if(array_key_exists($field,$c))<div><label class="text-xs font-bold">{{ $label }}</label>@if(in_array($field,['body','description'],true))<textarea wire:model.live.debounce.300ms="sections.{{ $sIndex }}.content.{{ $field }}" rows="4" class="mt-2 w-full rounded-xl border-zinc-200 text-sm dark:border-zinc-700 dark:bg-zinc-800"></textarea>@else<input wire:model.live.debounce.300ms="sections.{{ $sIndex }}.content.{{ $field }}" class="mt-2 w-full rounded-xl border-zinc-200 text-sm dark:border-zinc-700 dark:bg-zinc-800">@endif</div>@endif
                        @endforeach
                        @if(isset($c['items']) && is_array($c['items']))
                            <div class="border-t border-zinc-100 pt-5 dark:border-zinc-800"><div class="flex items-center justify-between"><div><p class="text-xs font-black">Itens</p><p class="mt-1 text-[10px] text-zinc-400">Adiciona e organiza conteúdos repetidos.</p></div><button type="button" wire:click="addItem({{ $sIndex }})" class="rounded-lg bg-indigo-50 px-2.5 py-1.5 text-[10px] font-bold text-indigo-700">+ Adicionar</button></div><div class="mt-3 space-y-3">@foreach($c['items'] as $itemIndex=>$item)<div class="rounded-xl border border-zinc-200 p-3 dark:border-zinc-700"><div class="flex items-center justify-between"><span class="text-[10px] font-black text-zinc-400">Item {{ $itemIndex+1 }}</span><div class="flex gap-1"><button type="button" wire:click="moveItem({{ $sIndex }},{{ $itemIndex }},'up')" class="px-1 text-xs">↑</button><button type="button" wire:click="moveItem({{ $sIndex }},{{ $itemIndex }},'down')" class="px-1 text-xs">↓</button><button type="button" wire:click="removeItem({{ $sIndex }},{{ $itemIndex }})" class="px-1 text-xs text-red-600">×</button></div></div>@foreach($item as $key=>$value)@if(!is_array($value))<label class="mt-2 block text-[10px] font-bold text-zinc-500">{{ \Illuminate\Support\Str::headline($key) }}<input wire:model.live.debounce.300ms="sections.{{ $sIndex }}.content.items.{{ $itemIndex }}.{{ $key }}" class="mt-1 w-full rounded-lg border-zinc-200 text-xs dark:border-zinc-700 dark:bg-zinc-800"></label>@endif @endforeach</div>@endforeach</div></div>
                        @endif
                        <div class="border-t border-zinc-100 pt-5 dark:border-zinc-800"><p class="text-xs font-black">Secção</p><div class="mt-3 grid grid-cols-2 gap-2"><button type="button" wire:click="moveSection({{ $sIndex }},'up')" class="rounded-lg border p-2 text-xs">↑ Subir</button><button type="button" wire:click="moveSection({{ $sIndex }},'down')" class="rounded-lg border p-2 text-xs">↓ Descer</button><button type="button" wire:click="duplicateSection({{ $sIndex }})" class="rounded-lg border p-2 text-xs">Duplicar</button><button type="button" wire:click="removeSection({{ $sIndex }})" class="rounded-lg border border-red-200 p-2 text-xs text-red-600">Eliminar</button></div></div>
                    @endif
                @endif
                <div class="border-t border-zinc-100 pt-5 dark:border-zinc-800"><p class="text-xs font-black">Design do website</p><p class="mt-1 text-[10px] leading-4 text-zinc-400">Cores e largura usadas como base visual.</p><div class="mt-3 grid grid-cols-2 gap-2">@foreach(['primary'=>'Principal','secondary'=>'Secundária','background'=>'Fundo','text'=>'Texto'] as $key=>$label)<label class="text-[10px] font-bold">{{ $label }}<input type="color" wire:model.live="theme.{{ $key }}" class="mt-1 h-9 w-full cursor-pointer rounded-lg border"></label>@endforeach</div><label class="mt-3 block text-[10px] font-bold">Largura do conteúdo<select wire:model.live="theme.content_width" class="mt-1 w-full rounded-lg border-zinc-200 text-xs dark:border-zinc-700 dark:bg-zinc-800"><option value="960px">Compacta</option><option value="1200px">Normal</option><option value="1440px">Grande</option></select></label></div>
            </div>
        </aside>
    </div>

    <div class="border-t border-zinc-200 bg-white px-3 py-2 lg:hidden dark:border-zinc-800 dark:bg-zinc-900"><div class="flex gap-2"><button type="button" @click="add=true" class="flex-1 rounded-xl bg-zinc-950 py-2 text-xs font-bold text-white">+ Adicionar</button><button type="button" @click="settings=true" class="flex-1 rounded-xl border py-2 text-xs font-bold">Personalizar</button><button type="button" wire:click="save" class="rounded-xl border px-4 py-2 text-xs font-bold">Guardar</button></div></div>

    <div x-cloak x-show="add" class="fixed inset-0 z-50 bg-zinc-950/40 lg:hidden" @click.self="add=false"><div class="absolute inset-x-0 bottom-0 max-h-[80vh] overflow-y-auto rounded-t-3xl bg-white p-5 dark:bg-zinc-900"><div class="mb-4 flex items-center justify-between"><div><p class="text-xs font-black uppercase tracking-widest text-indigo-600">Adicionar</p><h2 class="text-lg font-black">Escolhe um elemento</h2></div><button type="button" @click="add=false">✕</button></div>@foreach($elementGroups as $group=>$types)<p class="mb-2 mt-5 text-[10px] font-black uppercase tracking-widest text-zinc-400">{{ $group }}</p><div class="grid grid-cols-2 gap-2">@foreach($types as $type)<button type="button" wire:click="addSection('{{ $type }}')" @click="add=false" class="rounded-xl border p-3 text-left dark:border-zinc-700"><strong class="block text-xs">{{ \Illuminate\Support\Str::headline($type) }}</strong><span class="mt-1 block text-[10px] text-zinc-400">{{ $descriptions[$type] }}</span></button>@endforeach</div>@endforeach</div></div>

    <div x-cloak x-show="settings" class="fixed inset-0 z-50 bg-zinc-950/40 lg:hidden" @click.self="settings=false"><div class="absolute inset-x-0 bottom-0 max-h-[85vh] overflow-y-auto rounded-t-3xl bg-white p-5 dark:bg-zinc-900"><div class="mb-4 flex items-center justify-between"><div><p class="text-xs font-black uppercase tracking-widest text-indigo-600">Personalizar</p><h2 class="text-lg font-black">Editar conteúdo</h2></div><button type="button" @click="settings=false">✕</button></div><p class="text-sm text-zinc-500">Selecciona uma secção no website para editar os campos aqui.</p><div class="mt-4 space-y-3">@if(isset($sections[0]))@foreach(['title'=>'Título','subtitle'=>'Subtítulo','body'=>'Texto','description'=>'Descrição','button_label'=>'Botão'] as $field=>$label)@if(array_key_exists($field,$sections[$this->selectedSection??0]['content']??[]))<label class="block text-xs font-bold">{{ $label }}<input wire:model.live="sections.{{ $this->selectedSection??0 }}.content.{{ $field }}" class="mt-1 w-full rounded-xl border-zinc-200 text-sm"></label>@endif @endforeach @endif</div></div></div>

    @if($showOnboarding)
        <div class="fixed inset-0 z-[60] flex items-center justify-center bg-zinc-950/55 p-4"><div class="w-full max-w-xl rounded-3xl bg-white p-7 shadow-2xl dark:bg-zinc-900 sm:p-9"><div class="flex size-12 items-center justify-center rounded-2xl bg-indigo-600 text-white"><flux:icon name="sparkles" class="size-6" /></div><p class="mt-6 text-xs font-black uppercase tracking-widest text-indigo-600">Bem-vindo ao Builder</p><h2 class="mt-2 text-3xl font-black tracking-tight">Aqui é onde constróis o teu website.</h2><p class="mt-3 text-sm leading-6 text-zinc-500">Escolhe uma página, clica directamente num texto para o editar e usa o painel <strong>Personalizar</strong> para opções avançadas. Podes adicionar, duplicar, mover ou eliminar secções a qualquer momento.</p><div class="mt-6 grid gap-3 sm:grid-cols-3"><div class="rounded-2xl bg-zinc-50 p-4 dark:bg-zinc-800"><strong class="text-sm">1. Escolhe</strong><p class="mt-1 text-xs text-zinc-500">Uma página e uma secção.</p></div><div class="rounded-2xl bg-zinc-50 p-4 dark:bg-zinc-800"><strong class="text-sm">2. Edita</strong><p class="mt-1 text-xs text-zinc-500">Clica no conteúdo e escreve.</p></div><div class="rounded-2xl bg-zinc-50 p-4 dark:bg-zinc-800"><strong class="text-sm">3. Publica</strong><p class="mt-1 text-xs text-zinc-500">Pré-visualiza e publica.</p></div></div><div class="mt-7 flex justify-end"><button type="button" wire:click="dismissOnboarding" class="rounded-xl bg-zinc-950 px-5 py-3 text-sm font-bold text-white dark:bg-white dark:text-zinc-950">Percebi, vamos começar</button></div></div></div>
    @endif

    <div x-cloak x-show="help" class="fixed inset-0 z-[70] flex items-center justify-center bg-zinc-950/55 p-4" @click.self="help=false"><div class="w-full max-w-lg rounded-3xl bg-white p-7 shadow-2xl dark:bg-zinc-900"><div class="flex items-start justify-between"><div><p class="text-xs font-black uppercase tracking-widest text-indigo-600">Guia rápido</p><h2 class="mt-2 text-2xl font-black">Como funciona o Builder?</h2></div><button type="button" @click="help=false">✕</button></div><div class="mt-6 space-y-4 text-sm"><p><strong>Escolhe a página:</strong> usa a navegação de páginas para mudar entre Home, Serviços, Contacto e outras páginas.</p><p><strong>Edita directamente:</strong> clica num título, texto ou botão no website central e escreve por cima.</p><p><strong>Personaliza:</strong> selecciona uma secção para abrir as opções avançadas.</p><p><strong>Adiciona:</strong> escolhe novos elementos na coluna esquerda.</p><p><strong>Guarda e publica:</strong> o Builder mostra sempre se tens alterações por guardar.</p></div><button type="button" @click="help=false" class="mt-7 w-full rounded-xl bg-zinc-950 py-3 text-sm font-bold text-white">Percebi</button></div></div>

    <div x-cloak x-show="preview" class="fixed inset-0 z-[80] bg-zinc-950/80 p-3 sm:p-8"><div class="flex h-full flex-col overflow-hidden rounded-2xl bg-white"><div class="flex items-center justify-between border-b px-4 py-3"><strong class="text-sm">Pré-visualização — {{ $site->name }}</strong><button type="button" @click="preview=false" class="rounded-lg border px-3 py-1.5 text-xs font-bold">Fechar</button></div><iframe src="{{ route('site.public', ['site'=>$site, 'preview'=>1]) }}" class="min-h-0 flex-1 w-full border-0"></iframe></div></div>
</div>
