<div x-data="{ inspectorTab: 'content', add: false, settings: false, dragging: null }" class="flex h-screen min-h-0 flex-col overflow-hidden bg-zinc-100 text-zinc-900 dark:bg-zinc-950 dark:text-zinc-100">
    <header class="z-40 shrink-0 border-b border-zinc-200 bg-white/95 backdrop-blur dark:border-zinc-800 dark:bg-zinc-900/95">
        <div class="flex min-h-16 items-center gap-3 px-4 lg:px-6">
            <a href="{{ route('admin.site.dashboard', $site) }}" class="rounded-xl border border-zinc-200 px-3 py-2 text-sm font-bold hover:bg-zinc-50 dark:border-zinc-700 dark:hover:bg-zinc-800">← Sair</a>
            <div class="hidden min-w-0 lg:block"><p class="text-[10px] font-black uppercase tracking-widest text-indigo-600">Finder · Website Builder</p><p class="truncate text-sm font-black">{{ $site->name }}</p></div>
            <div class="ml-auto flex items-center gap-2">
                <select wire:model.live="pageId" wire:change="selectPage($event.target.value)" class="max-w-48 rounded-xl border-zinc-200 bg-white text-sm font-bold dark:border-zinc-700 dark:bg-zinc-800">
                    @foreach($site->pages->sortBy('sort_order') as $page)<option value="{{ $page->id }}">{{ $page->name }}{{ $page->is_homepage ? ' · Home' : '' }}</option>@endforeach
                </select>
                <button type="button" wire:click="createPage" class="hidden rounded-xl border px-3 py-2 text-xs font-bold lg:block">+ Página</button>
                <button type="button" wire:click="duplicatePage" class="hidden rounded-xl border px-3 py-2 text-xs font-bold xl:block">Duplicar</button>
                <button type="button" wire:click="undo" class="rounded-xl border px-3 py-2 text-xs font-bold" @disabled(!$this->canUndo)>↶</button>
                <button type="button" wire:click="redo" class="rounded-xl border px-3 py-2 text-xs font-bold" @disabled(!$this->canRedo)>↷</button>
                <a href="{{ route('site.public', ['site' => $site, 'preview' => 1]) }}" target="_blank" class="hidden rounded-xl border px-3 py-2 text-xs font-bold md:block">Pré-visualizar</a>
                <button type="button" wire:click="save" class="hidden rounded-xl border px-3 py-2 text-xs font-bold md:block">Guardar</button>
                <button type="button" wire:click="publish" class="rounded-xl bg-zinc-950 px-4 py-2 text-xs font-black text-white dark:bg-white dark:text-zinc-950">Publicar</button>
            </div>
        </div>
    </header>

    <div class="flex min-h-0 flex-1 overflow-hidden">
        <aside class="hidden h-full w-72 shrink-0 overflow-y-auto border-r border-zinc-200 bg-white lg:block dark:border-zinc-800 dark:bg-zinc-900">
            <div class="border-b border-zinc-100 p-5 dark:border-zinc-800"><p class="text-[10px] font-black uppercase tracking-widest text-indigo-600">1 · Adicionar</p><h2 class="mt-1 text-lg font-black">Construir página</h2><p class="mt-1 text-xs leading-5 text-zinc-500">Adiciona blocos e depois edita cada elemento directamente na página.</p></div>
            <div class="space-y-5 p-4">
                @foreach(['Conteúdo'=>['hero'=>'Hero / destaque','text'=>'Texto','image'=>'Imagem','button'=>'Botão','card'=>'Cartão'],'Estrutura'=>['feature_grid'=>'Benefícios','gallery'=>'Galeria','testimonials'=>'Testemunhos','faq'=>'Perguntas frequentes','pricing'=>'Preços'],'Negócio'=>['contact_form'=>'Formulário de contacto','product_grid'=>'Produtos','blog_posts'=>'Artigos','newsletter'=>'Newsletter','cta'=>'Call to action'],'Avançado'=>['video'=>'Vídeo','map'=>'Mapa','social_links'=>'Redes sociais']] as $group=>$types)
                    <div><p class="mb-2 px-1 text-[10px] font-black uppercase tracking-wider text-zinc-400">{{ $group }}</p><div class="grid gap-1.5">@foreach($types as $type=>$label)<button type="button" wire:click="addSection('{{ $type }}')" class="group flex items-center justify-between rounded-xl border border-zinc-200 px-3 py-3 text-left transition hover:border-indigo-300 hover:bg-indigo-50 dark:border-zinc-700 dark:hover:bg-zinc-800"><span class="text-xs font-bold">{{ $label }}</span><span class="text-zinc-300 group-hover:text-indigo-600">+</span></button>@endforeach</div></div>
                @endforeach
            </div>
        </aside>

        <main class="min-h-0 min-w-0 flex-1 overflow-y-auto">
            <div class="border-b border-zinc-200 bg-white px-4 py-3 dark:border-zinc-800 dark:bg-zinc-900"><div class="mx-auto flex max-w-[1440px] items-center gap-3"><div class="min-w-0 flex-1"><input wire:model.live.debounce.500ms="pageName" class="w-full border-0 bg-transparent p-0 text-lg font-black outline-none focus:ring-0"><p class="text-xs text-zinc-400">/{{ trim($pageSlug, '/') }} · {{ $pageStatus === 'published' ? 'Publicada' : 'Rascunho' }}</p></div><button type="button" wire:click="save" class="rounded-xl border px-4 py-2 text-xs font-black">Guardar agora</button><button type="button" wire:click="deletePage" class="rounded-xl border border-red-200 px-3 py-2 text-xs font-bold text-red-600">Eliminar</button></div></div>

            <div class="mx-auto max-w-[1440px] p-4 lg:p-8"><div class="mx-auto w-full max-w-[1200px] overflow-hidden rounded-2xl border border-zinc-200 bg-white shadow-sm dark:border-zinc-800 dark:bg-zinc-900"><div class="min-h-[650px]">
                @forelse($sections as $index=>$section)
                    @php($content=$section['content'] ?? [])
                    @php($items=$content['items'] ?? [])
                    <section data-section-id="{{ $section['id'] ?? 'new-'.$index }}" wire:key="builder-section-{{ $section['id'] ?? 'new-'.$index }}" wire:click="selectSection({{ $index }})" class="group relative border-b border-zinc-100 transition dark:border-zinc-800 {{ $section['is_visible'] ? '' : 'opacity-40' }} {{ $selectedSection === $index ? 'ring-2 ring-inset ring-indigo-500' : 'hover:ring-1 hover:ring-indigo-300' }}">
                        <div class="absolute left-3 top-3 z-20 hidden items-center gap-1 rounded-lg border border-zinc-200 bg-white/95 p-1 shadow-sm group-hover:flex dark:border-zinc-700 dark:bg-zinc-900/95"><button type="button" wire:click.stop="selectSection({{ $index }})" class="rounded px-2 py-1 text-[10px] font-bold">Editar</button><button type="button" wire:click.stop="moveSection({{ $index }},'up')" class="rounded px-2 py-1 text-[10px]">↑</button><button type="button" wire:click.stop="moveSection({{ $index }},'down')" class="rounded px-2 py-1 text-[10px]">↓</button><button type="button" wire:click.stop="duplicateSection({{ $index }})" class="rounded px-2 py-1 text-[10px]">Duplicar</button><button type="button" wire:click.stop="setSectionVisibility({{ $index }},{{ $section['is_visible'] ? 'false' : 'true' }})" class="rounded px-2 py-1 text-[10px]">{{ $section['is_visible'] ? 'Ocultar' : 'Mostrar' }}</button><button type="button" wire:click.stop="removeSection({{ $index }})" class="rounded px-2 py-1 text-[10px] text-red-600">Eliminar</button></div>
                        <div class="px-6 py-14 lg:px-16">
                            @if($section['type'] === 'hero')
                                <div class="mx-auto max-w-4xl text-center"><p class="mb-3 cursor-text text-xs font-black uppercase tracking-[.25em] hover:underline" style="color: {{ $theme['primary'] ?? '#635bff' }}" contenteditable="true" @click.stop @blur="$wire.updateInline({{ $index }}, 'subtitle', $event.target.innerText)">{{ $content['subtitle'] ?? 'Destaque' }}</p><h1 class="cursor-text text-4xl font-black tracking-tight hover:underline sm:text-6xl" contenteditable="true" @click.stop @blur="$wire.updateInline({{ $index }}, 'title', $event.target.innerText)">{{ $content['title'] ?? 'Título principal' }}</h1><p class="mx-auto mt-5 max-w-2xl cursor-text text-base leading-7 text-zinc-500 hover:underline" contenteditable="true" @click.stop @blur="$wire.updateInline({{ $index }}, 'description', $event.target.innerText)">{{ $content['description'] ?? $content['body'] ?? 'Apresenta aqui a tua empresa, produto ou serviço.' }}</p>@if(!empty($content['button_label']))<span class="mt-7 inline-flex cursor-text rounded-xl px-5 py-3 text-sm font-black text-white" style="background: {{ $theme['primary'] ?? '#635bff' }}" contenteditable="true" @click.stop @blur="$wire.updateInline({{ $index }}, 'button_label', $event.target.innerText)">{{ $content['button_label'] }}</span>@endif</div>
                            @elseif($section['type'] === 'text')
                                <div class="mx-auto max-w-3xl"><h2 class="cursor-text text-3xl font-black hover:underline" contenteditable="true" @click.stop @blur="$wire.updateInline({{ $index }}, 'title', $event.target.innerText)">{{ $content['title'] ?? 'Título' }}</h2><p class="mt-4 cursor-text whitespace-pre-line leading-7 text-zinc-500 hover:underline" contenteditable="true" @click.stop @blur="$wire.updateInline({{ $index }}, 'body', $event.target.innerText)">{{ $content['body'] ?? $content['description'] ?? '' }}</p></div>
                            @elseif($section['type'] === 'image')
                                <div class="mx-auto max-w-4xl text-center">@if(!empty($content['url']))<img src="{{ $content['url'] }}" alt="{{ $content['alt'] ?? '' }}" class="mx-auto max-h-[480px] rounded-2xl object-cover shadow-sm">@else<div class="rounded-2xl border-2 border-dashed border-zinc-200 py-24 text-sm text-zinc-400">Seleciona esta secção para adicionar uma imagem.</div>@endif@if(!empty($content['caption']))<p class="mt-3 cursor-text text-xs text-zinc-400 hover:underline" contenteditable="true" @click.stop @blur="$wire.updateInline({{ $index }}, 'caption', $event.target.innerText)">{{ $content['caption'] }}</p>@endif</div>
                            @elseif($section['type'] === 'cta')
                                <div class="rounded-3xl p-8 text-center sm:p-14" style="background: {{ $theme['background'] ?? '#f4f4f5' }}"><h2 class="cursor-text text-3xl font-black hover:underline" contenteditable="true" @click.stop @blur="$wire.updateInline({{ $index }}, 'title', $event.target.innerText)">{{ $content['title'] ?? 'Pronto para começar?' }}</h2><p class="mx-auto mt-3 max-w-2xl cursor-text text-zinc-500 hover:underline" contenteditable="true" @click.stop @blur="$wire.updateInline({{ $index }}, 'description', $event.target.innerText)">{{ $content['description'] ?? '' }}</p><span class="mt-6 inline-flex cursor-text rounded-xl px-5 py-3 text-sm font-black text-white" style="background: {{ $theme['primary'] ?? '#635bff' }}" contenteditable="true" @click.stop @blur="$wire.updateInline({{ $index }}, 'button_label', $event.target.innerText)">{{ $content['button_label'] ?? 'Começar' }}</span></div>
                            @elseif($section['type'] === 'product_grid')
                                <div><h2 class="cursor-text text-center text-3xl font-black hover:underline" contenteditable="true" @click.stop @blur="$wire.updateInline({{ $index }}, 'title', $event.target.innerText)">{{ $content['title'] ?? 'Produtos' }}</h2>@if(!empty($content['description']))<p class="mx-auto mt-3 max-w-2xl cursor-text text-center text-zinc-500 hover:underline" contenteditable="true" @click.stop @blur="$wire.updateInline({{ $index }}, 'description', $event.target.innerText)">{{ $content['description'] }}</p>@endif<div class="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">@forelse($items as $item)<article class="rounded-2xl border border-zinc-200 bg-white p-4 shadow-sm dark:border-zinc-700 dark:bg-zinc-900">@if(!empty($item['image_url']))<div class="mb-4 aspect-[4/3] overflow-hidden rounded-xl bg-zinc-100 dark:bg-zinc-800"><img src="{{ $item['image_url'] }}" alt="{{ $item['name'] ?? $item['title'] ?? '' }}" class="h-full w-full object-cover"></div>@endif<h3 class="font-black">{{ $item['name'] ?? $item['title'] ?? 'Produto' }}</h3><p class="mt-1 text-sm text-zinc-500">{{ $item['description'] ?? '' }}</p><div class="mt-3 flex items-center justify-between gap-2"><span class="font-black">{{ $item['price'] ?? '' }}</span><span class="text-[10px] text-zinc-400">{{ $item['category'] ?? 'Sem categoria' }}</span></div></article>@empty<div class="col-span-full rounded-2xl border-2 border-dashed border-zinc-200 py-12 text-center text-sm text-zinc-400">Ainda não existem produtos ativos. Adiciona-os em Produtos.</div>@endforelse</div></div>
                            @else
                                <div class="mx-auto max-w-4xl"><h2 class="cursor-text text-3xl font-black hover:underline" contenteditable="true" @click.stop @blur="$wire.updateInline({{ $index }}, 'title', $event.target.innerText)">{{ $content['title'] ?? \Illuminate\Support\Str::headline($section['type']) }}</h2>@if(!empty($content['description']))<p class="mt-4 cursor-text text-zinc-500 hover:underline" contenteditable="true" @click.stop @blur="$wire.updateInline({{ $index }}, 'description', $event.target.innerText)">{{ $content['description'] }}</p>@endif<div class="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">@forelse($items as $itemIndex=>$item)<article class="rounded-2xl border border-zinc-200 p-5 dark:border-zinc-700"><p class="cursor-text font-black hover:underline" contenteditable="true" @click.stop @blur="$wire.updateInline({{ $index }}, 'title', $event.target.innerText, {{ $itemIndex }})">{{ $item['title'] ?? $item['name'] ?? $item['question'] ?? $item['label'] ?? 'Item' }}</p><p class="mt-2 cursor-text text-sm leading-6 text-zinc-500 hover:underline" contenteditable="true" @click.stop @blur="$wire.updateInline({{ $index }}, 'description', $event.target.innerText, {{ $itemIndex }})">{{ $item['description'] ?? $item['quote'] ?? $item['answer'] ?? $item['excerpt'] ?? $item['price'] ?? '' }}</p></article>@empty<div class="col-span-full rounded-2xl border-2 border-dashed border-zinc-200 py-12 text-center text-sm text-zinc-400">Ainda não existem itens nesta secção.</div>@endforelse</div></div>
                            @endif
                        </div>
                    </section>
                @empty
                    <div class="flex min-h-[650px] items-center justify-center p-8 text-center"><div><div class="mx-auto flex h-14 w-14 items-center justify-center rounded-2xl bg-indigo-50 text-2xl">✦</div><h2 class="mt-5 text-xl font-black">Começa a construir a tua página</h2><p class="mx-auto mt-2 max-w-md text-sm leading-6 text-zinc-500">Escolhe uma secção no painel esquerdo. O conteúdo aparece aqui e pode ser personalizado sem código.</p></div></div>
                @endforelse
            </div></div>
        </main>

        <aside class="hidden h-full w-[420px] shrink-0 overflow-y-auto border-l border-zinc-200 bg-white lg:block dark:border-zinc-800 dark:bg-zinc-900">
            <div class="sticky top-0 z-20 border-b border-zinc-200 bg-white/95 backdrop-blur dark:border-zinc-800 dark:bg-zinc-900/95"><div class="p-5"><p class="text-[10px] font-black uppercase tracking-[.18em] text-indigo-600">3 · Personalizar</p><h2 class="mt-1 text-lg font-black">Conteúdo da página</h2><p class="mt-2 text-xs leading-5 text-zinc-500">Seleciona uma secção para editar todos os campos que existem realmente nessa secção.</p></div>@if($selectedSection !== null && isset($sections[$selectedSection]))<div class="grid grid-cols-2 border-t border-zinc-100 dark:border-zinc-800"><button type="button" @click="inspectorTab='content'" :class="inspectorTab==='content' ? 'border-indigo-500 text-indigo-600' : 'border-transparent text-zinc-500'" class="border-b-2 px-4 py-3 text-[10px] font-black uppercase">Conteúdo</button><button type="button" @click="inspectorTab='items'" :class="inspectorTab==='items' ? 'border-indigo-500 text-indigo-600' : 'border-transparent text-zinc-500'" class="border-b-2 px-4 py-3 text-[10px] font-black uppercase">Itens</button></div>@endif</div>
            @if($selectedSection !== null && isset($sections[$selectedSection]))
                @php($selectedContent=$sections[$selectedSection]['content'] ?? [])
                @php($selectedItems=$selectedContent['items'] ?? [])
                @php($selectedType=$sections[$selectedSection]['type'] ?? '')
                <div x-show="inspectorTab==='content'" class="space-y-4 p-5">
                    <div class="rounded-2xl border border-indigo-100 bg-indigo-50/60 p-4 dark:border-indigo-900 dark:bg-indigo-950/30"><p class="text-xs font-black">Edição directa</p><p class="mt-1 text-[11px] leading-5 text-zinc-500">Também podes clicar nos textos do canvas e escrever directamente.</p></div>
                    @forelse($selectedContent as $fieldKey=>$fieldValue)
                        @continue($fieldKey === 'items')
                        @php($fieldLabel=\Illuminate\Support\Str::headline(str_replace('_',' ',(string)$fieldKey)))
                        @if(is_scalar($fieldValue) || $fieldValue === null)
                            <div><label class="text-xs font-bold">{{ $fieldLabel }}</label>@if(strlen((string)$fieldValue)>100 || in_array($fieldKey,['body','description','answer','quote','excerpt'],true))<textarea wire:model.live.debounce.500ms="sections.{{ $selectedSection }}.content.{{ $fieldKey }}" rows="4" class="mt-2 w-full rounded-xl border-zinc-200 bg-zinc-50 px-3 py-3 text-sm dark:border-zinc-700 dark:bg-zinc-800"></textarea>@else<input wire:model.live.debounce.500ms="sections.{{ $selectedSection }}.content.{{ $fieldKey }}" class="mt-2 w-full rounded-xl border-zinc-200 bg-zinc-50 px-3 py-3 text-sm dark:border-zinc-700 dark:bg-zinc-800">@endif</div>
                        @elseif(is_array($fieldValue))
                            <div class="rounded-2xl border border-zinc-200 p-4 dark:border-zinc-700"><p class="text-xs font-black">{{ $fieldLabel }}</p><div class="mt-3 space-y-2">@foreach($fieldValue as $childKey=>$childValue)@if(is_scalar($childValue) || $childValue === null)<input wire:model.live.debounce.500ms="sections.{{ $selectedSection }}.content.{{ $fieldKey }}.{{ $childKey }}" class="w-full rounded-xl border-zinc-200 px-3 py-2 text-sm dark:border-zinc-700 dark:bg-zinc-800" placeholder="{{ is_numeric($childKey) ? 'Item '.((int)$childKey+1) : \Illuminate\Support\Str::headline(str_replace('_',' ',(string)$childKey)) }}">@endif @endforeach</div></div>
                        @endif
                    @empty<div class="rounded-2xl border-2 border-dashed border-zinc-200 p-8 text-center text-xs text-zinc-400">Esta secção ainda não tem campos.</div>@endforelse
                </div>
                <div x-show="inspectorTab==='items'" class="space-y-4 p-5">
                    @if($selectedType === 'product_grid')
                        <div class="rounded-2xl border border-emerald-200 bg-emerald-50/70 p-4 dark:border-emerald-900 dark:bg-emerald-950/20"><p class="text-xs font-black text-emerald-800 dark:text-emerald-300">Produtos reais do website</p><p class="mt-1 text-[11px] leading-5 text-emerald-700/80 dark:text-emerald-400">Esta secção é alimentada automaticamente pelos produtos ativos deste website. Não existem itens fictícios.</p><a href="{{ route('admin.site.products', $site) }}" class="mt-3 inline-flex rounded-xl bg-emerald-700 px-3 py-2 text-[10px] font-black text-white">Gerir produtos</a></div>
                    @else
                        <div class="flex items-center justify-between gap-3"><div><p class="text-xs font-black">Itens</p><p class="mt-1 text-[11px] text-zinc-400">Todos os campos reais de cada item aparecem abaixo.</p></div><button type="button" wire:click="addItem({{ $selectedSection }})" class="rounded-xl bg-zinc-950 px-3 py-2 text-[10px] font-black text-white dark:bg-white dark:text-zinc-950">+ Item</button></div>
                    @endif
                    @forelse($selectedItems as $itemIndex=>$item)
                        <div class="rounded-2xl border border-zinc-200 p-4 dark:border-zinc-700"><div class="mb-3 flex items-center justify-between"><span class="text-[10px] font-black uppercase tracking-wider text-zinc-400">{{ $selectedType === 'product_grid' ? 'Produto' : 'Item' }} {{ $itemIndex+1 }}</span>@if($selectedType !== 'product_grid')<div class="flex gap-1"><button type="button" wire:click="moveItem({{ $selectedSection }},{{ $itemIndex }},'up')" class="rounded-lg border px-2 py-1 text-[10px]">↑</button><button type="button" wire:click="moveItem({{ $selectedSection }},{{ $itemIndex }},'down')" class="rounded-lg border px-2 py-1 text-[10px]">↓</button><button type="button" wire:click="removeItem({{ $selectedSection }},{{ $itemIndex }})" class="rounded-lg border border-red-200 px-2 py-1 text-[10px] text-red-600">Eliminar</button></div>@endif</div>
                            @if($selectedType === 'product_grid')
                                @if(!empty($item['image_url']))<img src="{{ $item['image_url'] }}" alt="{{ $item['name'] ?? '' }}" class="mb-3 h-24 w-full rounded-xl object-cover">@endif
                                <div class="space-y-2">@foreach($item as $productKey=>$productValue)@if(is_scalar($productValue) || $productValue === null)<div class="flex items-start justify-between gap-3 rounded-xl bg-zinc-50 px-3 py-2 dark:bg-zinc-800"><span class="text-[10px] font-bold text-zinc-500">{{ \Illuminate\Support\Str::headline(str_replace('_',' ',(string)$productKey)) }}</span><span class="text-right text-xs font-semibold">{{ $productValue === '' || $productValue === null ? '—' : $productValue }}</span></div>@endif @endforeach</div>
                            @else
                                @foreach($item as $itemKey=>$itemValue)
                                    @php($itemLabel=\Illuminate\Support\Str::headline(str_replace('_',' ',(string)$itemKey)))
                                    @if(is_scalar($itemValue) || $itemValue === null)<div class="mb-3"><label class="text-[10px] font-bold text-zinc-500">{{ $itemLabel }}</label>@if(strlen((string)$itemValue)>100 || in_array($itemKey,['description','body','answer','quote','excerpt'],true))<textarea wire:model.live.debounce.500ms="sections.{{ $selectedSection }}.content.items.{{ $itemIndex }}.{{ $itemKey }}" rows="3" class="mt-1 w-full rounded-xl border-zinc-200 px-3 py-2 text-sm dark:border-zinc-700 dark:bg-zinc-800"></textarea>@else<input wire:model.live.debounce.500ms="sections.{{ $selectedSection }}.content.items.{{ $itemIndex }}.{{ $itemKey }}" class="mt-1 w-full rounded-xl border-zinc-200 px-3 py-2 text-sm dark:border-zinc-700 dark:bg-zinc-800">@endif</div>@elseif(is_array($itemValue))<div class="mb-3 rounded-xl border border-zinc-200 p-3 dark:border-zinc-700"><p class="text-[10px] font-black">{{ $itemLabel }}</p>@foreach($itemValue as $childKey=>$childValue)@if(is_scalar($childValue) || $childValue === null)<input wire:model.live.debounce.500ms="sections.{{ $selectedSection }}.content.items.{{ $itemIndex }}.{{ $itemKey }}.{{ $childKey }}" class="mt-2 w-full rounded-lg border-zinc-200 px-3 py-2 text-xs dark:border-zinc-700 dark:bg-zinc-800" placeholder="{{ is_numeric($childKey) ? 'Item '.((int)$childKey+1) : \Illuminate\Support\Str::headline(str_replace('_',' ',(string)$childKey)) }}">@endif @endforeach</div>@endif
                                @endforeach
                            @endif
                        </div>
                    @empty<div class="rounded-2xl border-2 border-dashed border-zinc-200 p-8 text-center text-xs text-zinc-400">Ainda não existem itens nesta secção.</div>@endforelse
                </div>
            @else
                <div class="flex min-h-[420px] items-center justify-center p-8 text-center"><div><div class="mx-auto flex h-12 w-12 items-center justify-center rounded-2xl bg-zinc-100 text-xl dark:bg-zinc-800">✦</div><h3 class="mt-4 font-black">Seleciona uma secção</h3><p class="mt-2 text-xs leading-5 text-zinc-500">Clica numa secção no centro para abrir todo o conteúdo editável.</p></div></div>
            @endif
        </aside>
    </div>

    <div class="fixed inset-x-0 bottom-4 z-50 flex justify-center px-4 lg:hidden"><div class="flex w-full max-w-md gap-2 rounded-2xl border border-zinc-200 bg-white/95 p-2 shadow-2xl backdrop-blur dark:border-zinc-700 dark:bg-zinc-900/95"><button type="button" @click="add=true" class="flex-1 rounded-xl bg-zinc-950 px-3 py-3 text-xs font-black text-white dark:bg-white dark:text-zinc-950">1 · Adicionar</button><button type="button" @click="settings=true" class="flex-1 rounded-xl border px-3 py-3 text-xs font-black">3 · Personalizar</button></div></div>
    <div x-show="add" x-cloak class="fixed inset-0 z-[70] bg-black/40 p-4 lg:hidden" @click.self="add=false"><div class="mx-auto mt-16 max-w-lg rounded-3xl bg-white p-5 shadow-2xl dark:bg-zinc-900"><div class="flex items-center justify-between"><h3 class="font-black">Adicionar secção</h3><button type="button" @click="add=false" class="rounded-xl border px-3 py-2">✕</button></div><div class="mt-5 grid grid-cols-2 gap-2">@foreach(['hero'=>'Hero','text'=>'Texto','image'=>'Imagem','feature_grid'=>'Benefícios','gallery'=>'Galeria','testimonials'=>'Testemunhos','faq'=>'FAQ','pricing'=>'Preços','contact_form'=>'Contacto','product_grid'=>'Produtos','blog_posts'=>'Artigos','newsletter'=>'Newsletter','cta'=>'CTA'] as $type=>$label)<button type="button" wire:click="addSection('{{ $type }}'); add=false" class="rounded-xl border px-3 py-3 text-left text-xs font-bold">{{ $label }}</button>@endforeach</div></div></div>
</div>