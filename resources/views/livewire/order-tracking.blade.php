<div class="mb-2"><x-site-workspace-selector :current-site="$site" route-name="orders" /></div><div class="space-y-6 p-6 lg:p-8"><div class="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><p class="text-sm font-medium text-indigo-600">Área do cliente</p><h1 class="mt-1 text-2xl font-semibold tracking-tight">Acompanhamento de encomendas</h1><p class="mt-1 text-sm text-zinc-500">Consulta aqui o estado e os artigos das tuas encomendas.</p></div><a href="{{ route('products') }}" wire:navigate class="rounded-xl border border-zinc-200 bg-white px-4 py-2.5 text-sm font-semibold hover:bg-zinc-50">Ver produtos</a></div><div class="space-y-4">@forelse($orders as $order)<article class="rounded-2xl border border-zinc-200 bg-white shadow-sm dark:border-zinc-800 dark:bg-zinc-900"><div class="flex flex-col gap-3 border-b border-zinc-100 p-5 sm:flex-row sm:items-center sm:justify-between dark:border-zinc-800"><div><p class="font-semibold">#{{ $order->order_number }}</p><p class="mt-1 text-xs text-zinc-500">{{ optional($order->sold_at)->format('d/m/Y H:i') }}</p></div><div class="sm:text-right"><p class="font-semibold">€ {{ number_format((float)$order->total,2,',','.') }}</p><span class="text-xs text-zinc-500">{{ ucfirst(str_replace('_',' ',$order->status)) }}</span></div></div><div class="grid gap-2 p-5 md:grid-cols-5">@foreach(['criada'=>'Criada','pago'=>'Pago','em_preparacao'=>'Em preparação','enviado'=>'Enviado','entregue'=>'Entregue'] as $step=>$label)<div class="rounded-xl border p-3 {{ $this->isStepComplete($order,$step) ? 'border-emerald-200 bg-emerald-50' : 'border-zinc-200 bg-zinc-50 dark:border-zinc-700 dark:bg-zinc-800' }}"><p class="text-xs font-semibold {{ $this->isStepComplete($order,$step) ? 'text-emerald-700' : 'text-zinc-500' }}">{{ $this->isStepComplete($order,$step) ? '✓ ' : '' }}{{ $label }}</p></div>@endforeach</div><div class="border-t border-zinc-100 px-5 pb-5 dark:border-zinc-800"><h2 class="pt-5 text-sm font-semibold">Artigos</h2><div class="divide-y divide-zinc-100 dark:divide-zinc-800">@foreach($order->items as $item)<div class="flex justify-between gap-4 py-3 text-sm"><span>{{ $item->product_name ?? $item->name ?? 'Produto' }} × {{ $item->quantity }}</span><span class="font-medium">€ {{ number_format((float)($item->total ?? $item->subtotal ?? 0),2,',','.') }}</span></div>@endforeach</div></div></article>@empty<div class="rounded-2xl border border-dashed border-zinc-300 bg-white p-12 text-center dark:border-zinc-700 dark:bg-zinc-900"><div class="text-3xl">📦</div><h2 class="mt-3 font-semibold">Ainda não tens encomendas</h2><p class="mt-1 text-sm text-zinc-500">As tuas encomendas aparecerão aqui.</p></div>@endforelse</div></div>